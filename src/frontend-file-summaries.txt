{
  "frontend/src/App.tsx": "Main application component with header, authentication controls, HTTP logging instructions card, latest reading card with manual refresh button, and all readings table with automatic 3-second polling plus manual refresh capability.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for fetching all readings and latest reading from the backend with 3-second polling intervals, exposing query keys for easy manual refetch/invalidation.",
  "frontend/src/lib/format.ts": "Utility functions to format numeric values (supporting both decimal numbers and bigint) and convert nanosecond timestamps to readable English date/time strings.",
  "frontend/src/index.css": "Global styles with warm neutral color palette (beige/tan tones) and emerald green accent using OKLCH color system for both light and dark modes.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom font families (Inter for sans, JetBrains Mono for monospace) and extended theme tokens.",
  "frontend/src/components/HttpLoggingInstructionsCard.tsx": "Instructions card component that uses the runtime config resolver to display backend HTTP endpoints with loading states, improved error messaging for configuration failures, retry button, and copyable URLs for logging and retrieving readings.",
  "frontend/src/lib/runtimeConfig.ts": "Runtime configuration resolver that determines backend canister ID through a robust fallback chain (build-time env, window global, session cache, env.json fetch with timeout) without hard-failing when env.json is unavailable, includes diagnostic utilities and caching.",
  "frontend/src/config.ts": "Core actor creation utility that uses the runtime config resolver to obtain the backend canister ID and creates an authenticated or anonymous actor with proper agent configuration and root key fetching for local development.",
  "frontend/src/hooks/useActor.ts": "React Query hook for actor initialization that integrates with Internet Identity, uses the config resolver for robust canister ID resolution, includes retry logic, and invalidates dependent queries when the actor changes.",
  "frontend/public/env.json": "Static fallback environment configuration file containing backend canister ID mapping structure, served from the frontend to provide a 200 response when the runtime config resolver attempts to fetch it.",
  "frontend/index.html": "HTML entry point with optional runtime configuration hook comment showing how hosting environments can inject window.CANISTER_ID_BACKEND for non-breaking configuration override."
}